# TEST_SPEC.md — kaguya-lander ループ別テストケース表

各テスト ID はテストコード内のテスト名に対応する。未着手ループ分は `test.fails` で
Red を封入してあり、ループ開始時(段階 3)に当該ループ分のみマークを外す。

## Loop 1 — REQ-001 物理エンジン(tests/physics.test.ts)

| ID | 検証内容 | 要点 |
|----|----------|------|
| T1-1 | 自由落下 | 入力なし・1 秒(60 step)後、vy ≈ GRAVITY×1、直立姿勢を維持 |
| T1-2 | 直立全推力 | angle=0 で thrust、1 秒後 vy ≈ (GRAVITY−THRUST_ACCEL)×1 で上昇加速 |
| T1-3 | 燃料消費と枯渇 | 全推力 1 秒で fuel −10。fuel=0.05 から噴射しても fuel は 0 で止まり負にならない |
| T1-4 | 燃料切れで推力無効 | fuel=0 で thrust しても自由落下と同一軌道 |
| T1-5 | 回転 | rotate=1 を 0.5 秒で angle ≈ +45。angle の [-180,180) 正規化 |
| T1-6 | 純関数性 | step が引数 state を破壊しない(呼出前後で deep equal) |
| T1-7 | 傾斜推力の分解 | angle=90 で thrust 時、ax ≈ +THRUST_ACCEL、ay 成分は重力のみ |

## Loop 2 — REQ-002 地形生成(tests/terrain.test.ts)

| ID | 検証内容 | 要点 |
|----|----------|------|
| T2-1 | 決定論 | 同一 (seed, difficulty) の 2 回呼び出しが deep equal |
| T2-2 | シード感度 | seed 1 と 2 で points が一致しない |
| T2-3 | 形状制約 | 51 点・x 昇順・間隔 8m・y が SPEC §4 のクランプ範囲内 |
| T2-4 | パッド構成 | difficulty ごとの本数・幅・倍率が SPEC §4 の表どおり(Loop 7 で beginner を追加) |
| T2-5 | パッド平坦性と離隔 | 各パッド区間内の点の y が同一値。パッド間距離 ≥ 24m |

## Loop 3 — REQ-003 判定とスコア(tests/judge.test.ts)

| ID | 検証内容 | 要点 |
|----|----------|------|
| T3-1 | 軟着陸成功 | パッド中央・vy=1.5, vx=0.5, angle=5 → 'landed' + 当該 multiplier |
| T3-2 | 速度超過で墜落 | vy=2.5(他は良好)→ 'crashed'。vx=1.2 でも同様 |
| T3-3 | 姿勢不良で墜落 | angle=12(他は良好)→ 'crashed' |
| T3-4 | パッド外接地は墜落 | 条件良好でも斜面上 → 'crashed'。片脚だけパッド外も 'crashed' |
| T3-5 | 場外逸脱 | x<0 / x>WORLD_W / y<0 → 'crashed' |
| T3-6 | スコア式 | fuel=50, vy=1.0, ×2 パッド → (50+250+25)×2 = 650。crashed は 0 |
| T3-7 | 飛行中は判定なし | 地形より十分上空では 'flying' のまま |

## Loop 4〜6 — REQ-004〜006(目視検証チェックリスト)

ユニットテスト対象外。PR の Preview URL で以下を確認し、結果を PR 本文に記録する。

- [ ] 60fps 相当で滑らかに描画され、タブ非アクティブ復帰後も物理が破綻しない
- [ ] キーボード・タッチ双方で操作できる(モバイル実機 1 台以上)
- [ ] タイトル → プレイ → リザルト → タイトルの遷移が一巡する
- [ ] HUD の高度・速度・燃料・角度が engine の状態と一致する
- [ ] 難易度 1/2/3/4 でパッド構成が変わる(1 = 超エントリー、Loop 7 以降)
- [ ] 墜落・着陸の演出とスコア表示が SPEC §6 と一致する
